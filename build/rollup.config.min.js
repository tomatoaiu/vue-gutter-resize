import { uglify } from 'rollup-plugin-uglify'

import plugins from './rollup.config.plugins'
plugins.push(uglify())

export default {
  input: './src/index.js',
  output: [
    {
      file: './dist/vue-gutter-resize.min.js',
      format: 'cjs'
    },
    {
      file: './dist/vue-gutter-resize.umd.min.js',
      format: 'umd',
      name: 'VueGutterResize'
    }
  ],
  plugins: plugins
}
